# Superseded audio-provenance snapshot

This first native PASS is preserved unchanged except for this additive notice.
Its cue bytes are identical to v2, but its source manifest names an intermediate
fanfare-renderer hash that is no longer the committed renderer source.  The
runtime result itself remains valid.  `../command_complete_runtime_v2/` is the
authoritative reproducible checkpoint generated from the committed renderer;
do not use this directory as the latest reproduction entry point.
