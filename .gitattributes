# Windows installer split parts: binary, vendored (excluded from
# linguist language stats) so GitHub does not attempt line diffs on them.
dist/windows-installer/New-Era-Setup.exe.part_* binary linguist-vendored
dist/windows-installer/*.exe                    binary linguist-vendored
