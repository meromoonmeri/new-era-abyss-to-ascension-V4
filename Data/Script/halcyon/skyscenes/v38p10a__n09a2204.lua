-- GÉNÉRÉ par dev/tools/sky_compile_scenes.py — NE PAS ÉDITER À LA MAIN.
-- Scène canonique PMD Sky EU : SCRIPT/V38P10A/n09a2204.ssb (ROM sha256 1fa39d35…).
-- Dialogues 5 langues ROM embarqués ; conventions du pilote m01a0204.
local SkySceneKit = require 'halcyon.skyscenes.kit'
return function(hero, partner)
  SkySceneKit.reset_locks()
  SkySceneKit.run_routine(function() -- def 1 for object OBJECT_V38P10A1_516
    -- SetOutputAttribute(8) [neutre/état moteur]
    -- SetAnimation(2) [anim idle native]
    -- @label_0 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P10A1_516 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -360) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(60, 80))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 2 for object OBJECT_V38P10A4_519
    GAME:WaitFrames(50)
    -- SetAnimation(2) [anim idle native]
    -- SlidePositionOffset OBJECT_V38P10A4_519 [cible sans placement SSA zone: déplacement non joué]
    -- Destroy() [neutre/état moteur]
  end)
  SkySceneKit.run_routine(function() -- def 3 for object OBJECT_V38P10A3_518
    -- SetOutputAttribute(8) [neutre/état moteur]
    GAME:WaitFrames(40)
    -- SetAnimation(2) [anim idle native]
    -- @label_1 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P10A3_518 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -360) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(20, 25))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 4 for object OBJECT_V38P10A6_521
    GAME:WaitFrames(60)
    -- SetAnimation(2) [anim idle native]
    -- @label_2 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P10A6_521 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -480) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(35, 45))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 5 for object OBJECT_V38P10A8_523
    -- SetOutputAttribute(8) [neutre/état moteur]
    GAME:WaitFrames(170)
    -- SetAnimation(2) [anim idle native]
    -- @label_3 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P10A8_523 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -480) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(60, 80))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 6 for object OBJECT_V38P10A2_517
    -- SetOutputAttribute(8) [neutre/état moteur]
    -- SetAnimation(2) [anim idle native]
    -- @label_4 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P10A2_517 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -600) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(85, 90))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 7 for object OBJECT_V38P10A7_522
    -- SetAnimation(2) [anim idle native]
    -- SetOutputAttribute(8) [neutre/état moteur]
    -- @label_5 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P10A7_522 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -300) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(50, 55))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 8 for object OBJECT_V38P10A5_520
    -- SetAnimation(2) [anim idle native]
    -- @label_6 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P10A5_520 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -480) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(40, 45))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 9 for object OBJECT_V38P10A9_524
    -- SetAnimation(2) [anim idle native]
    -- SetOutputAttribute(8) [neutre/état moteur]
    -- @label_7 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P10A9_524 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -480) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(35, 40))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 10 for object OBJECT_V38P08B2_503
    -- SetOutputAttribute(8) [neutre/état moteur]
    GAME:WaitFrames(95)
    -- SetAnimation(2) [anim idle native]
    -- SlidePositionOffset OBJECT_V38P08B2_503 [cible sans placement SSA zone: déplacement non joué]
    -- Destroy() [neutre/état moteur]
  end)
  SkySceneKit.run_routine(function() -- def 11 for object OBJECT_V38P08D6_515
    -- SetAnimation(2) [anim idle native]
    -- SetOutputAttribute(8) [neutre/état moteur]
    -- @label_8 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P08D6_515 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -480) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(60, 80))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 12 for object OBJECT_V38P08D5_514
    GAME:WaitFrames(40)
    -- SetAnimation(2) [anim idle native]
    -- @label_9 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P08D5_514 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -480) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(40, 60))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 13 for object OBJECT_V38P08D4_513
    -- SetAnimation(2) [anim idle native]
    -- SetOutputAttribute(8) [neutre/état moteur]
    -- @label_10 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P08D4_513 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -480) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(40, 60))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 14 for object OBJECT_V38P08C3_506
    -- SetOutputAttribute(8) [neutre/état moteur]
    GAME:WaitFrames(195)
    -- SetAnimation(2) [anim idle native]
    -- SlidePositionOffset OBJECT_V38P08C3_506 [cible sans placement SSA zone: déplacement non joué]
    -- Destroy() [neutre/état moteur]
  end)
  SkySceneKit.run_routine(function() -- def 15 for object OBJECT_V38P08C1_504
    -- SetOutputAttribute(8) [neutre/état moteur]
    GAME:WaitFrames(270)
    -- SetAnimation(2) [anim idle native]
    -- @label_11 [étiquette de flux ExplorerScript]
    -- forever{...} NDS: une itération compilée (ré-affichage du menu = annulation, documenté)
    -- SlidePositionOffset OBJECT_V38P08C1_504 [cible sans placement SSA zone: déplacement non joué]
    -- SetPositionOffset(0, -480) [routine décor sans sujet résolu]
    GAME:WaitFrames(math.random(50, 60))
    -- continue [contrôle de boucle forever NDS: une itération compilée]
    -- may be redundant
  end)
  SkySceneKit.run_routine(function() -- def 16 for object OBJECT_V38P08C5_508
    GAME:WaitFrames(210)
    -- SetOutputAttribute(8) [neutre/état moteur]
    -- SetAnimation(2) [anim idle native]
    -- SlidePositionOffset OBJECT_V38P08C5_508 [cible sans placement SSA zone: déplacement non joué]
    -- Destroy() [neutre/état moteur]
  end)
  -- bgm2_PlayFadeIn(BGM_EARTHQUAKE3) [canal BGM sub: PMDO n'a qu'un canal; hors TOP_FOCUS le principal garde la main - adaptation documentée]
  -- back_SetGround(LEVEL_V38P10A) [neutre/état moteur]
  -- supervision_Acting(0) [neutre/état moteur]
  -- camera_SetMyself() [neutre/état moteur]
  pcall(function() local g=GAME:GetCurrentGround(); GAME:MoveCamera(g.ViewCenter.X+(480), g.ViewCenter.Y+(360), 120, false) end) -- back_SetBackScrollSpeed(4.0,3.0) px/frame -> caméra continue (pilote scroll)
  pcall(function() GROUND:MoveScreen(RogueEssence.Content.ScreenMover(0, 2, 30)) end) -- camera_SetEffect('1', '1', '1', '0')
  GAME:FadeIn(30)
  GAME:WaitFrames(180)
  -- bgm2_FadeOut [canal BGM sub: voir bgm2_Play]
  -- GAP: se_Play(6403) — id SE NDS sans portage PMDO identifié
  -- screen2_WhiteOut [sub déjà caché]
  GAME:FadeOut(true, 60) -- screen_WhiteOut
  -- camera_SetEffect(0): arrêt d'effet caméra
  -- back_SetBackScrollSpeed(0,0): arrêt du défilement
  GAME:WaitFrames(2) -- join WaitSe
  GAME:WaitFrames(60)
  SkySceneKit.join_routines()
end
